# .env.example

# MongoDB connection URI (replace with your own credentials when creating the actual .env file)
MONGO_URI=mongodb://<username>:<password>@<host>:<port>/<database_name>

# Express server port (defaults to 5000 if not specified)
PORT=5002
